# FINAL CORRECTED WEBOSCKET PROXY FOR DOCAVAILABLE.ORG
# Add these blocks inside your 'server { listen 443; ... }' block in /etc/nginx/sites-available/docavailable.org

location /call-signaling {
    proxy_pass http://localhost:8080;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
}

location /audio-signaling {
    proxy_pass http://localhost:8080;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
}

location /chat-signaling {
    proxy_pass http://localhost:8080;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
}

location /webrtc-health {
    proxy_pass http://localhost:8080/webrtc-health;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
}
