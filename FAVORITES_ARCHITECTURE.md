# Favorites Feature - Architecture & Design

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    Patient Dashboard                         │
│  (app/patient-dashboard.tsx)                                │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ State Management                                       │ │
│  │ ├─ showFavoritesOnly: boolean                         │ │
│  │ ├─ favoriteDoctors: Doctor[]                          │ │
│  │ └─ favoritesRefreshTrigger: number                    │ │
│  └────────────────────────────────────────────────────────┘ │
│                           ↓                                   │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ Render Discover Page                                  │ │
│  │ ├─ Search Bar with Bookmark Button                   │ │
│  │ ├─ Filter & Sort Options                             │ │
│  │ └─ Doctor Cards List                                 │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
         ↓                                      ↓
    ┌─────────────────┐            ┌──────────────────────┐
    │  Doctor Card    │            │ Bookmark Button      │
    │ (Component)     │            │ (Search Bar)         │
    │                 │            │                      │
    │ ┌─────────────┐ │            │ ┌────────────────┐   │
    │ │ Bookmark    │ │            │ │ Toggle Favs    │   │
    │ │ Icon Button │ │            │ │ View           │   │
    │ └─────────────┘ │            │ └────────────────┘   │
    └─────────────────┘            └──────────────────────┘
         ↓                                    ↓
    ┌─────────────────────────────────────────────────────┐
    │  Favorite Doctors Service                          │
    │  (services/favoriteDoctorsService.ts)             │
    │                                                    │
    │  ├─ addFavorite()                                 │
    │  ├─ removeFavorite()                              │
    │  ├─ getFavorites()                                │
    │  ├─ isFavorite()                                  │
    │  ├─ getFavoritesCount()                           │
    │  └─ clearFavorites()                              │
    └─────────────────────────────────────────────────────┘
              ↓
    ┌─────────────────────────────────────────────────────┐
    │  AsyncStorage                                      │
    │  Key: 'favorite_doctors'                           │
    │  Format: JSON Array<FavoriteDoctor>               │
    └─────────────────────────────────────────────────────┘
```

---

## Data Flow Diagram

### Adding to Favorites

```
┌──────────────────────┐
│ User clicks bookmark │
│ icon on doctor card  │
└──────────────────────┘
         ↓
┌──────────────────────────────────────┐
│ handleFavoritePress() triggered      │
│ - Stop propagation                   │
│ - Check current favorite status      │
└──────────────────────────────────────┘
         ↓
┌──────────────────────────────────────┐
│ favoriteDoctorsService.addFavorite() │
│ - Get existing favorites             │
│ - Check if already favorited         │
│ - Add new favorite                   │
│ - Save to AsyncStorage               │
└──────────────────────────────────────┘
         ↓
┌──────────────────────────────────────┐
│ Update DoctorCard state              │
│ - setIsFavorite(true)                │
│ - Icon turns green                   │
└──────────────────────────────────────┘
         ↓
┌──────────────────────────────────────┐
│ Call onFavoriteChange callback       │
│ - Increment favoritesRefreshTrigger  │
└──────────────────────────────────────┘
         ↓
┌──────────────────────────────────────┐
│ loadFavoriteDoctors() runs           │
│ - Fetch favorites from service       │
│ - Map to full doctor objects         │
│ - Update favoriteDoctors state       │
└──────────────────────────────────────┘
         ↓
┌──────────────────────────────────────┐
│ filteredAndSortedDoctors recalculates│
│ - Updates memoized value             │
│ - Triggers re-render                 │
└──────────────────────────────────────┘
         ↓
┌──────────────────────────────────────┐
│ UI Updates                           │
│ - Bookmark badge shows count         │
│ - Doctor card shows filled icon      │
└──────────────────────────────────────┘
```

### Toggling Favorites View

```
┌────────────────────────────────┐
│ User clicks bookmark button    │
│ in search bar                  │
└────────────────────────────────┘
         ↓
┌────────────────────────────────┐
│ setShowFavoritesOnly(true/false)│
└────────────────────────────────┘
         ↓
┌────────────────────────────────────────┐
│ filteredAndSortedDoctors recalculates  │
│                                        │
│ if (showFavoritesOnly) {               │
│   filteredDoctors = favoriteDoctors    │
│ } else {                               │
│   filteredDoctors = doctors            │
│ }                                      │
└────────────────────────────────────────┘
         ↓
┌────────────────────────────────┐
│ Apply other filters/search     │
│ - Online status                │
│ - Specialization               │
│ - Search query                 │
│ - Sort order                   │
└────────────────────────────────┘
         ↓
┌────────────────────────────────┐
│ UI Updates                     │
│ - Show/hide favorite doctors   │
│ - Animate transitions          │
│ - Update button color          │
└────────────────────────────────┘
```

---

## Component Hierarchy

```
PatientDashboard
├── Search Bar
│   ├── Search Input
│   ├── Clear Button
│   ├── Bookmark Button (NEW)
│   └── Filter Button
├── Active Filters Display
└── Doctors List
    ├── DoctorCard 1
    │   ├── Profile Picture
    │   ├── Doctor Info
    │   ├── Bookmark Icon (NEW)
    │   └── Chevron
    ├── DoctorCard 2
    │   ├── Profile Picture
    │   ├── Doctor Info
    │   ├── Bookmark Icon (NEW)
    │   └── Chevron
    └── ... more cards
```

---

## State Management Flow

```
┌─────────────────────────────────────────────────────────┐
│ Patient Dashboard State                                 │
└─────────────────────────────────────────────────────────┘
    ↓
    ├─ showFavoritesOnly: boolean
    │  └─ Controls: Which doctors to display
    │
    ├─ favoriteDoctors: Doctor[]
    │  └─ Contains: Full doctor objects that are favorited
    │
    └─ favoritesRefreshTrigger: number
       └─ Triggers: loadFavoriteDoctors() when incremented

┌─────────────────────────────────────────────────────────┐
│ Doctor Card State                                       │
└─────────────────────────────────────────────────────────┘
    ↓
    └─ isFavorite: boolean
       └─ Controls: Bookmark icon appearance
```

---

## Filtering Pipeline

```
All Doctors (doctors[])
    ↓
┌─────────────────────────────────────────┐
│ Step 1: Select Source                   │
│ if (showFavoritesOnly)                  │
│   → Use favoriteDoctors[]               │
│ else                                    │
│   → Use doctors[]                       │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ Step 2: Filter by Online Status         │
│ if (showOnlyOnline)                     │
│   → Keep only is_online === true        │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ Step 3: Filter by Specialization        │
│ if (selectedSpecialization)             │
│   → Keep matching specialization        │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ Step 4: Filter by Search Query          │
│ if (searchQuery)                        │
│   → Keep matching name/spec/location    │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ Step 5: Sort                            │
│ switch (sortBy)                         │
│   → name, rating, experience, etc.      │
└─────────────────────────────────────────┘
    ↓
Filtered & Sorted Doctors (filteredAndSortedDoctors[])
```

---

## UI Layout Diagram

### Search Bar

```
┌─────────────────────────────────────────────────────────┐
│  [🔍] [Search doctors...] [✕] [📑] [⚙️]               │
│                                 ↑    ↑                  │
│                           Bookmark  Filter              │
│                           Button     Button             │
│                           (NEW)                         │
└─────────────────────────────────────────────────────────┘

Bookmark Button States:
  Inactive: [📑] gray, no badge
  Active:   [📑] green, with count badge
```

### Doctor Card

```
┌─────────────────────────────────────────────────────────┐
│  [👤] Dr. John Doe              [📑] [→]              │
│       Cardiology • 10+ years                            │
│       Languages: English, Spanish                       │
│                                  ↑                      │
│                           Bookmark Icon (NEW)           │
│                           Gray when not favorited       │
│                           Green when favorited          │
└─────────────────────────────────────────────────────────┘
```

---

## Color Scheme

### Primary Colors
- **Active/Favorited**: `#4CAF50` (Green)
- **Inactive/Not Favorited**: `#CCC` (Light Gray)
- **Badge Active**: `#4CAF50` (Green)
- **Badge Inactive**: `#E8F5E8` (Very Light Green)

### Icon States

```
Not Favorited          Favorited
┌──────────────┐      ┌──────────────┐
│ [📑] Gray    │  →   │ [📑] Green   │
│ Color: #CCC  │      │ Color: #4CAF50│
└──────────────┘      └──────────────┘
```

---

## Performance Optimization

### Memoization Strategy

```
filteredAndSortedDoctors = useMemo(() => {
  // Expensive filtering and sorting operations
  return result;
}, [
  doctors,
  favoriteDoctors,
  showFavoritesOnly,
  showOnlyOnline,
  selectedSpecialization,
  searchQuery,
  sortBy
]);

// Only recalculates when dependencies change
```

### Lazy Loading

```
Doctors loaded
    ↓
useEffect triggered
    ↓
loadFavoriteDoctors() called
    ↓
Favorites loaded from AsyncStorage
    ↓
favoriteDoctors state updated
    ↓
UI renders with favorite status
```

---

## Error Handling Flow

```
User Action
    ↓
Try Block
├─ Execute operation
├─ Update state
└─ Call callback
    ↓
Catch Block (if error)
├─ Log error to console
├─ Graceful fallback
└─ UI remains stable
```

---

## AsyncStorage Schema

```json
{
  "favorite_doctors": [
    {
      "id": 1,
      "name": "Dr. John Doe",
      "specialization": "Cardiology",
      "profile_picture_url": "https://example.com/pic.jpg",
      "savedAt": 1700000000000
    },
    {
      "id": 2,
      "name": "Dr. Jane Smith",
      "specialization": "Neurology",
      "profile_picture_url": "https://example.com/pic2.jpg",
      "savedAt": 1700000001000
    }
  ]
}
```

---

## Integration Points

### With Existing Features

```
Favorites Feature
    ├─ Search Filter ✓
    ├─ Online Filter ✓
    ├─ Specialization Filter ✓
    ├─ Sorting ✓
    ├─ Doctor Details ✓
    ├─ Animations ✓
    └─ Appointments ✓
```

### With External Services

```
Favorites Feature
    ├─ AsyncStorage (persistence)
    ├─ Icon Service (bookmark icons)
    └─ Doctor Service (doctor data)
```

---

## Testing Strategy

### Unit Tests
- Service methods (add, remove, get, check)
- State updates
- Filter logic

### Integration Tests
- Bookmark button interaction
- Favorites view toggle
- Filter combinations

### E2E Tests
- Add to favorites
- View favorites
- Remove from favorites
- Persistence across sessions

---

**Architecture Version**: 1.0
**Last Updated**: November 17, 2025
